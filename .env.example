# Copy to .env (or export in your shell) and fill values.
CONTEXT7_API_KEY=
SUPABASE_ACCESS_TOKEN=
OPENAI_API_KEY=

# App runtime env vars (Vite exposes only VITE_* vars to client code).
VITE_APP_NAME=codex-vue-starter
VITE_API_BASE_URL=http://localhost:3000
