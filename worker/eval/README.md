# Eval

This folder is eval-only. It is not part of the live `/api/chat` runtime path.

- `runners/`: executable eval scripts
- `lib/`: shared eval utilities
- `config/`: optimize auto-apply rule files
- `datasets/`: local golden datasets
- `artifacts/`: eval output files
